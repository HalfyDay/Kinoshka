<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Холодный старт и системный сплэш (API 31+): чёрный фон вместо светлого DayNight. -->
    <style name="Theme.Kino" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:windowSplashScreenBackground">@android:color/black</item>
    </style>
    <style name="Theme.mpvex" parent="Theme.Material3.DynamicColors.DayNight.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:windowSplashScreenBackground">@android:color/black</item>
    </style>
</resources>