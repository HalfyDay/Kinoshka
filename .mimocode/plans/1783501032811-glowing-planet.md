# Plan: Outlined navigation bar icons (filled on selection)

## Summary
Change the bottom navigation bar icons from always-filled to outlined-by-default, filling them when selected.

## Current State
`BottomSectionBar` in `HomeScreen.kt:1698` uses 3 `BottomSectionButton` composables, each passing a static `Icon` with filled variants:
- `Icons.AutoMirrored.Filled.List` (Library)
- `Icons.Filled.Search` (Discover)
- `Icons.Filled.MoreVert` (More)

`NavItemGlyph` (line 1788) already animates background, tint, and scale on selection — but the icon vector itself never changes.

## Changes

### File: `app/src/main/java/hd/kinoshka/app/ui/screens/HomeScreen.kt`

1. **Modify `BottomSectionButton`** to accept `selectedIcon: ImageVector` and `unselectedIcon: ImageVector` instead of `icon: @Composable () -> Unit`
2. **Modify `NavItemGlyph`** to accept the same two vectors and switch between them based on `selected` state
3. **Update `BottomSectionBar`** call sites to pass outlined + filled variants:
   - Library: `Icons.Outlined.List` / `Icons.AutoMirrored.Filled.List`
   - Discover: `Icons.Outlined.Search` / `Icons.Filled.Search`
   - More: `Icons.Outlined.MoreVert` / `Icons.Filled.MoreVert`

## Verification
1. Build succeeds
2. Bottom bar shows outlined icons when no section is selected (or current section is not highlighted)
3. Selected section icon is filled
4. Transition between outlined ↔ filled is smooth (already animated via `NavItemGlyph`)
